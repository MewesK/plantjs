{
  "name": "plantjs",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --open",
    "build": "tsc && vite build",
    "preview": "tsc && vite build && vite preview --host --open"
  },
  "dependencies": {
    "babylonjs": "^7.7.0"
  },
  "devDependencies": {
    "typescript": "latest",
    "vite": "latest"
  },
  "typeRoots": [
    "node_modules/@types",
    "src/@types"
  ]
}
